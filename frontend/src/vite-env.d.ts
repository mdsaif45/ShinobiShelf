/// <reference types="vite/client" />

// Provides the types for `import.meta.env` (PROD, DEV, MODE, ...) which Vite
// injects at build time. Without this reference TypeScript does not know the
// `env` property exists on ImportMeta.
